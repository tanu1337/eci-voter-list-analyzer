# Google Gemini API Keys (JSON array format - REQUIRED)
# Get your API keys from: https://aistudio.google.com/app/apikey
# Use multiple keys for parallel processing and better performance
GEMINI_API_KEYS=["your-api-key-1","your-api-key-2","your-api-key-3"]

# Processing Configuration (Optional - defaults shown)
# Number of PDF pages per chunk (1-10, recommended: 5)
MAX_PAGES_PER_CHUNK=5

# Number of API requests before taking a break
REQUESTS_BEFORE_BREAK=10

# Break duration in milliseconds (60000 = 1 minute)
BREAK_DURATION_MS=60000

# Directory Configuration (Optional - defaults shown)
# Temporary directory for PDF chunks
TEMP_DIR=./temp_chunks

# Output directory for JSON results
OUTPUT_DIR=./results

# Example with multiple API keys for better performance:
# GEMINI_API_KEYS=["AIzaSyA...","AIzaSyB...","AIzaSyC..."]

# Performance tuning examples:
# For faster processing with larger chunks:
# MAX_PAGES_PER_CHUNK=8
# REQUESTS_BEFORE_BREAK=15

# For rate limit sensitive scenarios:
# MAX_PAGES_PER_CHUNK=3
# REQUESTS_BEFORE_BREAK=5
# BREAK_DURATION_MS=120000
